<div class="container-fluid">
  <div class="row g-0">
    <div class="col-md-6 form-section">
      <div class="form-container">
        <h2 class="heading">Register</h2>
        <p class="text">Silahkan Untuk Mendaftar</p>
        <form method="POST" action="/register">
          <div class="input-group mb-3 shadow-sm">
            <input
              type="text"
              class="form-control"
              name="nama"
              placeholder="Nama"
              required
            />
          </div>
          <div class="input-group mb-3 shadow-sm">
            <input
              type="email"
              class="form-control"
              name="email"
              placeholder="Email"
              required
            />
          </div>
          <div class="input-group mb-3 shadow-sm">
            <select class="form-select" name="role" required>
              <option value="employee" selected>Employee</option>
              <option value="manager">Manager</option>
              <option value="editor">Editor</option>
            </select>
          </div>
          <div class="input-group mb-3 shadow-sm">
            <input
              type="password"
              class="form-control"
              name="password"
              placeholder="Password"
              required
            />
            <span
              class="input-group-text toggle-password"
              onclick="togglePassword(this)"
            >
              <i class="bi bi-eye"></i>
            </span>
          </div>
          <div class="input-group mb-3 shadow-sm">
            <input
              type="password"
              class="form-control"
              name="confirm_password"
              placeholder="Confirm Password"
              required
            />
            <span
              class="input-group-text toggle-password"
              onclick="togglePassword(this)"
            >
              <i class="bi bi-eye"></i>
            </span>
          </div>
          <button type="submit" class="btn btn-login w-100 shadow-sm">
            Submit
          </button>
        </form>
        <p class="mt-3 text-center">
          Jika Sudah Terdaftar, <a href="/login">Silahkan Login</a>
        </p>
      </div>
    </div>

    <!-- Right Section -->
    <div class="col-md-6 right-section d-none d-md-flex">
      <div class="overlay">
        <h3 class="heading">Selamat Datang Di Aplikasi Workspace</h3>
        <p class="text">
          Sambutan hangat untuk Anda yang baru bergabung di platform manajemen
          perkantoran terdepan.
        </p>
      </div>
    </div>
  </div>
</div>
